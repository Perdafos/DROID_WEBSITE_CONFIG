# DroidCam source stream URL
DROIDCAM_URL=http://192.168.1.100:4747/video

# Server config
PORT=3000
HOST=0.0.0.0

# Basic auth (optional — uncomment to enable)
# USERNAME=admin
# PASSWORD=rahasia123

# Relay mode: "proxy" (default) — server fetches & relays
#             "direct" — server tells viewers the source IP
RELAY_MODE=proxy
